services:
  - type: web
    name: re-puestos
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: COOKIE_SECRET
        generateValue: true
      - key: DEBUG
        value: "false"
