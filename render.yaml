services:
  - type: web
    name: re-puestos
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    envVars:
      - key: COOKIE_SECRET
        generateValue: true
      - key: DEBUG
        value: "false"
